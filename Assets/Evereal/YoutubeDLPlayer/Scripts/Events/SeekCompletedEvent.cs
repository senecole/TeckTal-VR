namespace Evereal.YoutubeDLPlayer
{
  /// <summary>
  /// Invoke after a seek operation completes.
  /// </summary>
  /// <param name="instance">The <c>YTDLPlayer</c> source that is emitting the event.</param>
  public delegate void SeekCompletedEvent(YTDLPlayer instance);
}
